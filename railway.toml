[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/healthz"
restartPolicyType = "on_failure"

# NOTE: The Volume is created out-of-band with `railway volume add --mount-path /data`
# (Railway does not provision volumes from this file). SQLite lives at /data/notes.db.
